import '../titleBar.css';

export default function TitleBar() {
  return (
    <nav className="fixed z-50 top-0 left-0 right-0 h-8 text-sm bg-main-bg-dark flex justify-start items-center pl-3 border-b border-b-slate-400 titlebar">
      Vocab-It
    </nav>
  )
}
